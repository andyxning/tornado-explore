# netutil
----------

##  Desc
----------

## Notices

### resolver
1. `resolver`也是继承自`Configurable`，默认使用的`BlockingResolver`。
  * `BlockingResolver`
  * `ThreadedResolver`
  * `OverrideResolver`
  * `twisted.TwistedResolver`
  * `caresresolver.CaresResolver`
